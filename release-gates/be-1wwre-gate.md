# Release gate — be-1wwre (Fix: Test isolation — 5 tests fail under ambient BEADS_ACTOR and global git config beads.role)

**Date:** 2026-09-03
**Deployer:** beads/deployer
**Bead (deploy):** be-1wwre — Deploy Review: Test isolation: 5 tests fail under ambient BEADS_ACTOR and global git config beads.role
**Source bead:** be-klbl6 (build), reviewed by be-cemdo — verdict: pass
**Source commit:** `32408035ce4a76194c0fcb4cb8fbd1a920042ded` (provenance branch `builder/be-klbl6`, review bead be-cemdo)
**Branch:** `deploy/be-1wwre-gate` (isolated, cut fresh at the reviewed SHA — never the shared `builder/be-klbl6` branch)
**Base:** `origin/main` @ `c0d8da42d` ("fix(storage/uow): retry transient ping failures during openDB bootstrap (#6003)")
**Merge-base:** `c0d8da42d` — identical to origin/main tip; clean fast-forward, 0 commits behind
**Merge-tree simulation:** `git merge-tree --write-tree origin/main 32408035c` → tree `e45fa62a1`, exit 0, **zero conflicts**

## Verdict: PASS — all 7 criteria clear

## Criteria walk

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Review PASS present | PASS | be-cemdo notes: `verdict: pass` (sub-review beads be-e922u security, be-9q1jr spec, both closed). Style clean (`gofmt -l`: clean; `go vet`: clean). Security clean — full 9-point OWASP walk against the 3-file/+16/-0 diff, all N/A (test-scoped `t.Setenv` env mutation and `os.DevNull`-pointed git config isolation, no production code touched, no secrets). |
| 2 | Acceptance criteria met | PASS | Sole acceptance criterion — isolate 5 named tests (`TestCollectViperEntriesWithEnvOverride`, `TestPrepareSelectedCommandContext_RebindsTargetConfig`, `TestPrepareSelectedCommandContext_DoesNotMergeCallerConfigForUnsetKeys`, `TestCheckBeadsRole_NotConfigured`, `TestCheckBeadsRole_NotGitRepo`) from ambient `BEADS_ACTOR`/`BD_ACTOR` env vars and ambient global/system `git config beads.role` — is exercised, passing, and causally proven (see criterion 3). |
| 3 | Tests pass | PASS | Independently re-run by this deployer on the cut branch (not merely trusted from the review bead), **under this shell's own ambient contamination** (`BEADS_ACTOR=beads/deployer` set, `git config --global beads.role` = `contributor` — exactly the two conditions this diff isolates against): all 5 diff-owned tests PASS individually (`go test ./cmd/bd/... -run '<5 names>' -v`). Primary CI-equivalent full-suite (`make ci-pr-core`) also re-run independently: **97 packages, 0 FAIL, exit 0**, "pr-core go test succeeded in 529s" — including `cmd/bd` (356s) and `cmd/bd/doctor` (13s) explicitly green. Counts match be-cemdo's independently-reported evidence (be-9q1jr: "0 FAIL across ~100+ packages"). Causal verification (performed by the reviewer, be-cemdo/be-9q1jr): confirmed FAIL at merge-base under ambient conditions with exactly the isolation-bug symptoms this diff fixes; confirmed PASS at the reviewed commit under both ambient-set and ambient-unset conditions. |
| 3a | Pre-existing-failure attribution | N/A | `tests_green` true for the documented CI-equivalent run (both the reviewer's and this deployer's independent full-suite runs); the only environment where the pre-fix symptom appears is a raw/direct test invocation on this shared host carrying ambient `BEADS_ACTOR`/`beads.role` — which is the exact bug class this diff fixes, not an unrelated pre-existing failure. Tracking bead for the ambient-contention failure class generally: be-nu0z6 (unaffected by, and out of scope for, this diff). |
| 3b | Policy/lint lane | PASS (with pre-existing, out-of-scope exception) | `gofmt -l` on each changed file: clean (re-run independently). `go vet ./cmd/bd/...`: clean (re-run independently). Full `make ci-pr-lint` re-run independently by this deployer: fails only on 3 pre-existing `gosec G602` findings in `backend/conformance/{cycle_detector_contract,importer_contract,relations_contract}.go` — confirmed via `git diff --stat origin/main...32408035c` that this diff touches none of those files (3 files touched, all under `cmd/bd/`); these findings exist on `origin/main` unchanged by this branch and are out of scope. No tracking bead currently filed for this specific gosec class; not blocking since strictly pre-existing and unrelated to the diff. |
| 3c | CI-config-diff handling | N/A | No `.github/workflows/**` or other CI-config files in the diff — confirmed via `git diff --stat origin/main...32408035c`: 3 files, `cmd/bd/config_show_test.go`, `cmd/bd/context_binding_test.go`, `cmd/bd/doctor/role_test.go` only. |
| 4 | No HIGH-severity findings open | PASS | be-cemdo/be-e922u security_findings: none (full 9-point OWASP walk, all N/A). This deployer's independent lint re-run surfaced no findings inside the diff — only the pre-existing, out-of-scope `backend/conformance` gosec findings noted under 3b. |
| 5 | Final branch is clean | PASS | `git status --short` on `deploy/be-1wwre-gate` at `32408035c`: no tracked-file changes, clean working tree (verified after `go build`-equivalent full test run — no stray artifacts). |
| 6 | Branch diverges cleanly from main | PASS | Merge-base equals `origin/main` tip exactly (`c0d8da42d`, 0 commits behind — the cleanest possible base; same base as the prior be-c8kgv gate, confirming `origin/main` has not moved). `git merge-tree --write-tree origin/main 32408035c` succeeds with a single merged tree (`e45fa62a1`), exit 0, no conflict markers. |
| 7 | Single feature theme | PASS | `git diff --stat origin/main...32408035c`: 3 files, `cmd/bd/config_show_test.go` (+1), `cmd/bd/context_binding_test.go` (+4), `cmd/bd/doctor/role_test.go` (+11), 16 insertions total, 0 deletions. One theme throughout: neutralize two ambient-contamination vectors (`BEADS_ACTOR`/`BD_ACTOR` env vars; global/system `git config beads.role`) across 5 tests in two related groups (cmd/bd env isolation, cmd/bd/doctor git-config isolation), following the existing env-isolation idiom already used in `cmd/bd/doctor_context_test.go`. |

## Disposition

- **PR opened, not merged.** `gastownhall/beads` is a contributor-only repo for this rig (no push/merge rights) — the deployer's job ends at the open PR regardless of gate outcome. Per the standing `beads-pr-ours-mergeable-is-terminal` rule, no merge-request is routed to mayor for this repo once the PR is open (and mergeable); this is terminal from our side, awaiting upstream maintainer merge — no further contributor action.
- Gate is a clean PASS with no waivers, no substitutes, no open blockers. Small, additive-only diff (3 files, +16/-0), 0 divergence from origin/main, fully independently re-verified by this deployer — including a live re-run under this shell's own ambient contamination (`BEADS_ACTOR` set, global `beads.role` set), which directly reproduces the exact conditions the fix targets and confirms it holds.
