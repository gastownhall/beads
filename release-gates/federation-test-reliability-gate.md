# Release gate — be-mp5ag (Fix: federation tests hardcode main and share one database, so a nil deref SIGSEGVs the whole shard-5 binary)

**Date:** 2026-08-21
**Deployer:** beads/deployer
**Bead (deploy):** be-mp5ag — Deploy Review: Fix: federation tests hardcode main and share one database, so a nil deref SIGSEGVs the whole shard-5 binary
**Source bead:** be-eyvy2 — closed, review verdict: pass (round 3)
**Source commit:** `9c2776933d992d6cabd754dc52c9fa6a7003b9c1` (provenance branch `builder/be-dk4a`, build bead be-8zggi, review bead be-eyvy2)
**Branch:** `deploy/be-mp5ag-gate` (isolated, cut fresh at the reviewed SHA — never the shared `builder/be-dk4a` branch)
**Base:** `origin/main` @ `b7175be22e` ("test(storage/db): split out the search-counts behavioral coverage and add the cross-form parity test it was missing (#5938)")
**Merge-base:** `1617f3a85c` — 3 commits behind origin/main tip; those 3 commits touch unrelated files, confirmed via clean `git merge-tree` below; 0 self-rebase needed
**Merge-tree simulation:** `git merge-tree --write-tree origin/main 9c2776933` → tree `7d208ee32`, exit 0, **zero conflicts**
**PR:** (opening now — see bead be-mp5ag notes for the URL once routed)

## Verdict: PASS — all 7 criteria clear

## Criteria walk

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Review PASS present | PASS | be-eyvy2: verdict `pass`, closed with reason `pass` (round 3, single-pass — gemini second-pass disabled). Style clean (gofmt, go vet both independently reverified in an isolated scratch worktree). Security clean — full OWASP-Top-10 walk against the round's actual hunks, all "none" / "not applicable"; no production code touched (diff is test-file-only). |
| 2 | Acceptance criteria met | PASS | All 5 items in be-8zggi's exit_contract independently reverified by the reviewer at round 3 and cross-checked here: (i) `TestFederationDatabaseIsolation` passes under real ambient multi-agent contention (not an idle box); (ii) fix shape matches the ga-sui3p4 ruling — named, justified constant, not a bare literal bump; (iii) guard test enforces >=2x the replaced deadline with a rationale citing the concrete contention evidence; (iv) non-regression bar (VersionControlAPIs, ListRemotes, gofmt, go vet, test-file-only scope) held; (v) TDD shape (compile-failure RED at `cf3076c55`, live-server GREEN at `9c2776933`) confirmed. |
| 3 | Tests pass | PASS | Independently re-run by this deployer against a live podman-backed `dolthub/dolt-sql-server:2.2.0` testcontainer (not trusted from the reviewer's report): all 4 diff-owned tests — `TestFederationDatabaseIsolation`, `TestFederationIsolationTimeoutHasContentionHeadroom`, `TestFederationVersionControlAPIs`, `TestFederationListRemotes` — 4 PASS, 0 FAIL, 0 SKIP (53.7s). Then re-run again via the documented CI-equivalent full lane, `make ci-pr-core` (`go test -p4 -parallel4 -race -short -skip '^TestEmbedded' ./...`), with `BEADS_TEST_ENV_RUN_DOLT=1` / `DOCKER_HOST` / `TESTCONTAINERS_RYUK_DISABLED=1` set so the dolt-backed suite cannot silently self-skip: **ran 1010s, exit 1** — one package failure, `cmd/bd`, `panic: test timed out after 10m0s` at exactly 600.329s (Go's default per-binary deadline, no `-timeout` override in this lane). Every other package in `./...` passed, including — critically — the diff-owned package itself: `ok  internal/storage/dolt  193.695s`. No DATA RACE anywhere in the 2726-line log. `go test ./...` continued past the `cmd/bd` panic and ran the remaining ~80 packages to completion; `cmd/bd` was the sole failure. Matches the reviewer's own independent rerun in be-eyvy2 for the 4 diff-owned tests (same 4/0/0 counts, different scratch worktree). `failure_attribution: cmd/bd (panic: test timed out after 10m0s) -> tracked as bd-jxmz5 "scripts/test.sh 3m per-package timeout flakes internal/audit and cmd/bd on loaded machines" (explicitly: "reproduce identically on main"), corroborated by bd-1rh "cmd/bd test suite is absurdly slow - 279 tests taking 8+ minutes" (open, unresolved — the package's own baseline is already close to the 10m ceiling before any contention) and be-tum2/be-ayvl "...10min timeouts" (both reconfirmed 2026-08-21, same day, same rig: live ambient-Dolt-server cross-talk under concurrent ./... execution) + base-ref result: reproduced independently by this deployer. `git worktree add --detach` at `origin/main` (b7175be22e), same package, same core flags (`-p4 -parallel4 -race -short -skip '^TestEmbedded'`, explicit `-timeout 8m` this time for a bounded/deterministic check) → `FAIL github.com/steveyegge/beads/cmd/bd 484.295s`, `panic: test timed out after 8m0s`, same whole-binary-alarm mechanism (`testing.(*M).startAlarm.func1()`), same shape of goroutine dump (dozens parked in `chan receive`/`select`, e.g. `eventkit.(*Collector).drain`, `setupGracefulShutdown`), 0 DATA RACE. Actively-running test at panic differed (`TestInitDoltMetadata` @ 2m54s vs. the diff-branch run's `TestMultiIDUpdatePartialFailureExitsNonzero` @ 11s) — expected/immaterial, since which parallel subtest is "current" when a whole-binary alarm fires is scheduling-dependent, not diagnostic. Also observed one pre-existing non-timeout failure in the same BASE_REF run, `TestInit_WithBEADS_DIR_DoltBackend` (proxied-server metadata state bleed) — independent corroboration of cmd/bd's already-tracked cross-test pollution (bd-vb2u0, be-j8e), not investigated further as it's outside this attribution's scope.`. Clause (i) not diff-owned: satisfied, and structurally over-satisfied — the diff touches only `internal/storage/dolt/federation_test.go`, a `_test.go` file, which is categorically outside any package's importable API surface and cannot affect `cmd/bd` at runtime. Clause (ii) tracked bead id: satisfied via the four citations above (pre-existing, not filed fresh — already well-tracked). Clause (iii) proven pre-existing at BASE_REF: satisfied — empirically reproduced by this deployer, not merely inferred (see base-ref result above). Clause (iv) no path overlap: satisfied, mechanically confirmed via `git diff --name-only $(git merge-base origin/main HEAD) HEAD` → single file, `internal/storage/dolt/federation_test.go`. **All four clauses met — criterion 3 PASS via attribution, not waiver.** |
| 3b | Policy/lint lane | PASS | `make ci-pr-policy`: 8 of 9 steps PASS individually confirmed (build-tags, go-install-guidance, doc-flags, doc-freshness, testing-short, workapi-frontend-boundary, no-beads-jsonl-changes, `api-check`/httpapi tests). The 9th step (`check-versions.sh`) fails solely on `.githooks/commit-msg` missing BEADS INTEGRATION markers — verified this is a git-ignored, untracked, session-local shim (`worktree-setup.sh` artifact) that is not part of the beads repo at any commit, including the deploy SHA (`git cat-file -e 9c2776933:.githooks/commit-msg` fails — the path does not exist in that tree); not diff-owned, not repo state. `make ci-pr-lint` (`BD_LINT_NEW_FROM_MERGE_BASE=origin/main`, matching `pr.yml`'s actual invocation exactly): gofmt clean, golangci-lint 0 issues native + 0 issues Windows cross-lint. (An unscoped first pass surfaced 5 gosec findings; all 5 evaporated once scoped correctly — 2 were stale cache entries pointing at a sibling worktree, `../builder/worktrees/be-gepv/...`, confirmed absent from disk entirely; 3 were in `backend/conformance/*.go`, files this diff never touches, confirmed pre-existing on `origin/main`.) `make api-check`: PASS, no OpenAPI drift, `internal/httpapi` tests green. |
| 4 | No HIGH-severity findings open | PASS | be-eyvy2 `style_findings`: none. `security_findings`: none, across a full OWASP-Top-10 walk of the round's actual hunks. Zero findings of any severity open. |
| 5 | Final branch is clean | PASS | `assert_deploy_ancestry_scope origin/main 9c2776933d992d6cabd754dc52c9fa6a7003b9c1 be-mp5ag be-eyvy2 be-8zggi be-dk4a` → exit 0 (no `.claude/**` paths in the three-dot diff; all 5 non-merge commits in `origin/main..9c2776933` cite one of the passed bead ids in their full message body — 3 visible in the subject line already, the other 2 confirmed by re-checking `git log -1 --format='%B'` since the subject-only view missed a body-text mention (`9c2776933`: "be-8zggi round 2" in prose) and a `refs` trailer (`c4a03131c`: "refs be-dk4a, be-8zggi") respectively). `resolve_deploy_branch_target be-mp5ag 9c2776933d992d6cabd754dc52c9fa6a7003b9c1` → exit 0, branch `deploy/be-mp5ag-gate` checked out at exactly that SHA (never the shared `builder/be-dk4a` branch). `git status --short` on that branch: clean except the untracked gate-evidence file itself (this file, about to be committed). |
| 6 | Branch diverges cleanly from main | PASS | `git merge-tree --write-tree origin/main 9c2776933` succeeds with a single merged tree (`7d208ee32`), exit 0, no conflict markers. origin/main's 3 commits since the merge-base touch unrelated files. No self-rebase needed. |
| 7 | Single feature theme | PASS | `git diff --stat` over the full range: 1 file, `internal/storage/dolt/federation_test.go`, 145 insertions(+)/27 deletions(-). One theme throughout — federation test reliability: a nil-deref crash from checking out a hardcoded `"main"` instead of the test's real isolated branch, a database-isolation test that could only ever pass by accident (both towns silently shared one hardcoded database name), and a fixed timeout that reproducibly failed under real ambient host contention — root-caused together across be-dk4a/be-8zggi rounds 1-3 and fixed in the same file. |

## Disposition

- **PR opened, not merged.** `gastownhall/beads` is a contributor-only repo (no push/merge rights); the deployer's job ends at the open PR regardless of gate outcome — no merge-request routed, no waiting on mayor.
- Diff is test-file-only: no production code changed, matching the reviewer's own scope confirmation.
